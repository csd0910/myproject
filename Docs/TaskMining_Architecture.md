# NEXUS Task Mining - システムアーキテクチャ＆実装仕様書

## 1. システム全体のアプローチ（ユーザー要望に基づく基本思想）

**「クライアントは問答無用で全データを収集し、分析・AI処理はサーバー側で行う」**

個人のボトルネックを正確に分析するため、クライアントアプリ側でのフィルタリングやAI画像解析（Gemini API等）を廃止し、**「いかにPCに負荷をかけずに、ありとあらゆる生データを吸い上げ続けるか」**に特化するアーキテクチャに改修されました。これにより、真の意味での個人レベル・部門レベルでのデータ分析が可能となっています。

---

## 2. クライアント側（TaskMiningClient）の実装機能と裏側

クライアントは純粋な「テレメトリコレクター」として動作します。UI上の「START MINING」をONにすると、以下の情報が無条件で取得されます。

### 取得している生データ一覧
1. **アクティブウィンドウ情報 (MACRO)**
   * **内容**: 現在ユーザーが操作しているアプリ名（例: `EXCEL.EXE`）と、ウィンドウタイトル（例: `週次報告書.xlsx - Excel`）
   * **間隔**: 5秒に1回のポーリング
   * **会議の把握**: ZoomやMeetを開いている間はウィンドウタイトルとして記録されるため、サーバー側で会議時間として集計されます。
2. **クリップボード転記 (COPY_PASTE)**
   * **内容**: ユーザーがコピー・カットを行った瞬間のテキストデータ（先頭100文字）
   * **間隔**: 常時監視（変化があった瞬間にフック）
3. **キーボード・マウスの活動量 (INPUT_STATS)**
   * **内容**: 純粋な「キーボードを叩いた回数」と「マウスをクリックした回数」（入力内容自体は取得せず、プライバシーを保護）
   * **間隔**: 10秒ごとにカウントを合算して送信

### クライアント側の裏側処理・データフロー
* **ライブログビュー (LIVE LOG VIEW)**:
  * メモリを極限まで節約するため、ヘッダー等の装飾を削ぎ落としたプレーンテキストのストリーム表示を採用。
  * 表示が100行を超えると古い行から自動削除され、長時間の稼働でもPCが重くなりません。
* **ローカルキャッシュ＆非同期通信 (`db_client.py`)**:
  * 取得した生データは、ネットワーク遅延の影響を受けないよう**即座にローカルDB（SQLite）へ保存**されます。
  * 裏側で常駐する同期専用スレッドが、**10秒に1回**のペースで未送信データをサーバー（ポート8080）へバックグラウンド送信します（送信成功後にローカルから削除）。

---

## 3. サーバー側（TaskMiningServer）の役割

クライアントから送られてきた膨大な生データを集約し、ダッシュボード上で可視化・AI分析を行う役割を担います。

### 主な処理と構成
1. **データの集約と保存 (`app.py` / PostgreSQL)**:
   * 全社員のクライアントから飛んでくる生ログをAPIエンドポイントで受け取り、中央のデータベース（PostgreSQL）に保存します。
2. **ダッシュボードでの可視化 (`dashboard.html` / `user_dashboard.html`)**:
   * **KPI集計**: 生ログから「アイドル時間」「AIツール（ChatGPT等）の使用時間」「会議時間（Zoom/Meet等）」を集計し、カードとして表示します。
   * **フロー図生成**: 生ログの流れを解析し、どのファイルをどういう順番で触っているかをMermaid.jsを用いて動的なフロー図として表現します。
3. **（今後の拡張）ダッシュボード可視化時のAI分析**:
   * クライアント側にあったAI解析を取り払ったため、今後は「DBに溜まった生データを元に、ダッシュボード表示時やバッチ処理でAI（Gemini等）に分析させ、業務改善案を提示させる」という構成にシフトします。
