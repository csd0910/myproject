# Agent Handoff (Antigravity 2.0 連携用プロンプト)

## 今回のプロジェクト概要
現場が手作業で行っていたExcel/CSVのデータ加工業務を自動化するPythonツールを作成しています。
プロジェクト名：`UploadDataCreate`

## 完了した内容と意図（フェーズ1完了）
- 現場の担当者が作業内容を書き残したExcelの吹き出し（Shapes）をPythonで抽出し、業務プロセスを解読（`process_specification.md` を作成）。
- UIからの段階的実行を可能にするため、`main.py` の処理を `DataProcessor` クラスとしてリファクタリングし、全8工程（Stage 1〜8）のメソッドに分割しました。
- Tkinterを用いたステップバイステップ実行用のUI（`gui.py`）を作成し、`main.py` との結合を完了しました。ボタンを押すたびに各工程が実行され、都度 `temp_stageX.xlsx` という中間ファイルが出力されます。
- マージファイルの増減やイベント条件への柔軟な対応のため、各種設定を外部ファイルに分離し、UIから `config_sample.json` を読み込むアーキテクチャに変更しました。

### ⚠️ 重要方針・制約
- **ツールのゴールについて**: 本ツールの役割は「前処理から最終フォーマット調整を行い、`normal_item_result.xlsx` を出力すること」までです。
- **後続ツール（佐々嶌exeなど）との連動は不要**です。ファイル出力後はユーザーが手動で別ツールを使用するため、自動で別プログラムを呼び出すようなロジックは絶対に含めないでください。

## 関連ファイル・パス一覧
* **実装対象（メイン処理）**: `c:\Users\フォーレスト026\MyProject\UploadDataCreate\main.py`
* **実装対象（UI）**: `c:\Users\フォーレスト026\MyProject\UploadDataCreate\gui.py`
* **設定ファイルサンプル**: `c:\Users\フォーレスト026\MyProject\UploadDataCreate\config_sample.json`
* **要件定義書**: `c:\Users\フォーレスト026\MyProject\UploadDataCreate\process_specification.md`

---

## 次に行うべきタスク (Antigravity 2.0 へのお願い)
UIとロジックの結合（段階的実行）が完了したため、今後は以下の手順で内部のデータ処理ロジックの作り込みを進めてください。

1. **欠損工程の実装**:
   - `main.py` 内でスキップされている箇所（現場確認事項など）の実装や、より堅牢なデータ型変換・マッチングロジックの強化を行ってください。
2. **後半ロジック（Stage 5〜8）の実装**:
   - 現在プレースホルダー等になっている「3. 文字列操作・条件付与」（送料無料、イベント別キーワード付与）について、`config_sample.json` から読み込んだ情報を使って正確に処理するように実装してください。
3. **エラーハンドリング**:
   - 列が存在しなかった場合やJSONの形式違いなどによるクラッシュを防ぎ、UI側（`gui.py`）に適切なエラーメッセージを返せるようにしてください。

※ 一度に全て実装すると不具合の切り分けが難しくなるため、段階的にコードを構築してください。
