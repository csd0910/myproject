# 🤖 AGENT_HANDOFF (Antigravity 2.0 引継ぎ用ドキュメント)

## 1. 現在のプロジェクトと目的
- **プロジェクト**: `UploadDataCreate` (商品アップロード用データ作成の自動化ツール)
- **目的**: GUI (`gui.py`) から呼び出されるバックエンドのデータ処理ロジック (`main.py` の `DataProcessor`) が、担当者（尾島様）の手作業による正解データと完全に一致するかを、各工程（Stage1〜8）ごとに検証・デバッグしています。

## ⚠️【最重要原則】（必ず守ること）
1. **「元の吹き出し（コメント・メモ）」に基づくテストデータ**: 作業者本人は、元々あったExcel上の吹き出しの内容からこのテストデータを作成しています。その原則や背景を常に念頭に置いて推論してください。
2. **「本人作業分を絶対的な正（正解）」とする**: プログラムの出力が手作業のデータと食い違った場合、「手作業のデータが間違っている」と疑うのではなく、**「本人作業分のデータを『正』として、それに完全に一致するようにPythonの処理ロジック側を合わせ込む（調整する）」** ように作っていく予定です。差異が出た場合は、この前提でロジックを修正してください。

## 2. 使用している主要ファイル
- **実装コード**:
  - `main.py` : データ加工のコアルーチン（全8工程が実装済み）
  - `gui.py` : ユーザーが操作する対話型のUI
- **テスト・検証用スクリプト**:
  - `verify_all_stages.py` : 全工程の中間ファイル（`temp_stageX.xlsx`）と正解データを一括比較するスクリプト。
  - `test_step_by_step.py` : 特定のStageだけを、中間ファイルを読み込んで単体実行・テストするためのスクリプト。
- **データ群**:
  - `【テスト用】商品名作成 尾島手作業\元データ\` : 各Stageで読み込む入力ファイル群。
  - `【テスト用】商品名作成 尾島手作業\【手順X】...xlsx` : 尾島様が手作業で作成した各工程の正解ファイル。

## 3. 現在の進捗と直近で完了した修正（Context）
1. **Stage 1 (更新除外データの削除)**
   - テスト実行し、正解データとの完全一致（`[OK]`）を確認済み。
2. **Stage 2 (取り寄せ品の除外)**
   - 参照ファイル（`【手順4】111取り寄せ＋有効在庫有り.xlsx`）の対象列名が「商品コード」ではなく「品目コード」だったため、列の自動判定処理（`get_col_index`）を修正しました。
   - Stage 2の抽出条件（有効在庫数==0 または 未引当数>=1）について、仕様書の「条件をくぐり抜けて『生き残った』ものをベースから除外する」というルール通りに `~condition` で抽出するようロジックを修正済みです。

## 4. 発生している問題（残課題）
現在、Stage 2 の処理結果と正解データ（`【手順5】result_260819_154419.xlsx`）を比較すると、**Python側で約24件の「消し漏れ」** が発生し検証が `[NG]` となっています。
- **原因の有力な仮説**: 尾島様の手作業フォルダには、仕様書に記載されていない謎の工程ファイル `【手順2】result_...xlsx` が存在し、そこで手動で約25行が削除されています。Python側は仕様書（工程1の次は工程3・4の統合）通りに動いているため、この「謎の手順2」での削除分が考慮されておらず、差異として出ている可能性が高いです。

## 5. 次のエージェント（Antigravity 2.0）が実行すべきアクション
Antigravity 2.0 はこのファイルを確認後、以下のステップで作業を再開してください。

### Step 1: Stage 2 の現状の差異を確認する
以下のコマンドを実行し、Stage 2の単体テストと検証結果を出力して状況を把握してください。
```bash
cd C:\Users\フォーレスト026\MyProject\UploadDataCreate
python test_step_by_step.py
```

### Step 2: 差異の原因特定と修正
ユーザーと対話しながら、漏れている24件が「謎の手順2」に起因するものなのか、または `main.py` の `stage2_remove_stock_order` の条件ロジックがまだ足りないのかを分析・修正してください。
（※ヒント：`verify_all_stages.py` を改造して、漏れている品目cdのリストをprintさせると原因究明が早いです）

### Step 3: 次のステージへの進行
Stage 2 が `[OK]` になったら、`test_step_by_step.py` の中身を Stage 3 のテスト用に書き換え、同様に `[OK]` になるまでデバッグを繰り返してください。これを Stage 8 まで完遂することが最終目標です。
