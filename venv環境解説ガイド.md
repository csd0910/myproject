# 初心者SEのための Python 仮想環境 (.venv) 入門ガイド

* **本資料のチャットタイトル**: 【推奨タイトル：2026/07/15_venv環境と重さの違い】
* **作成日**: 2026年07月15日

---

## 1. 「.venv（仮想環境）」と「それ以外（グローバル環境）」の違い

Pythonでプログラムを作る際、ライブラリ（Excelを操作する `openpyxl` や、スクレイピングをする `pandas` など）をインストールします。この「ライブラリの保存場所」が異なります。

### 🏢 それ以外（グローバル環境）＝「全員で使う大部屋」
PC内のすべてのPythonスクリプトが、同じ大部屋にあるライブラリを使用します。
* **メリット**: 一度ライブラリをインストールすれば、どこにあるスクリプトからでも動かせます。
* **デメリット**: 
  * 「ツールAのためにライブラリを最新にアップデートしたら、大部屋の共通ライブラリが書き換わり、昔作ったツールBが動かなくなった」という**競合トラブル**が起きます。
  * いろいろなツールを入れるたびに大部屋が散らかり、管理ができなくなります。

### 🏠 .venv（仮想環境）＝「プロジェクト専用の個室」
プロジェクトのフォルダ内に `.venv` という専用の個室（フォルダ）を作り、そのツールが必要とするライブラリだけを隔離してインストールします。
* **メリット**: 
  * 他のツールに影響を与えたり、与えられたりすることがありません。
  * 「このツールを動かすには、このライブラリのこのバージョンが必要」という設定（`requirements.txt`）を他のPCへ簡単にコピー・再現できます。
* **デメリット**: 
  * プロジェクトごとに個室を作るため、少しだけハードディスクの容量を消費します。

---

## 2. 「PCを重くしている要因」と「venv」の関係

> [!IMPORTANT]
> **「venvを使っていないからPCが重くなる」ということはありません。**
> venvは単なる「ファイルの保存場所のルール」に過ぎないため、プログラム実行中のPCの重さ（CPUやメモリの消費量）は、venvでもグローバル環境でも**基本的には全く同じ**です。

### 💻 PCが重くなっている本当の要因
PCの動作が重くなっている場合、主に以下の原因が考えられます。

1. **裏で常駐しているスクリプト（無限ループ）がある**
   * 例：`BizStorage_Delay_Monitor.py`（15分おきの遅延監視）や、`audit_log_exporter.py`（イベントログ監視）などが、タスクスケジューラやスタートアップで**バックグラウンドで常に動き続けている**場合。
   * 特に、コード内の「待機処理（`time.sleep()`）」の書き方に問題があると、CPU使用率が100%近くまで跳ね上がることがあります。
2. **重い処理をグローバル環境で動かしている**
   * プログラム実行時の一時的なメモリ不足。

---

## 3. 今のプログラムを venv 環境に置き換える（実行する）方法

プログラムファイル（`.py`）自体を書き換える必要はありません。**実行するコマンドやバッチファイル（`.bat`）を少し変えるだけ**で、安全な venv 環境で動かすことができます。

現在、この `MyProject` フォルダには、すでに `.venv` という仮想環境が構築されています。

### 方法A. バッチファイル（.bat）で venv を指定して実行する（推奨）
タスクスケジューラやダブルクリックで自動実行している場合、バッチファイルの中身を以下のように記述することで、自動的に venv 内のきれいなPython環境で実行されます。

```bat
@echo off
cd /d "C:\Users\フォーレスト026\MyProject"
:: venv内のpython.exeを直接指定してスクリプトを動かす
".venv\Scripts\python.exe" "tools\generate_overtime_report_v3.py"
pause
```

### 方法B. コマンドプロンプトやPowerShellで実行する場合
手動で実行する場合は、まず仮想環境を「有効化（アクティベート）」してから実行します。

1. コマンドプロンプトでプロジェクトフォルダに移動します：
   ```cmd
   cd C:\Users\フォーレスト026\MyProject
   ```
2. venv環境に入ります（有効化）：
   ```cmd
   .venv\Scripts\activate
   ```
   ※画面の先頭に `(.venv)` と表示されれば成功です。
3. プログラムを実行します：
   ```cmd
   python tools\generate_overtime_report_v3.py
   ```
4. 終わったら、環境を抜けます：
   ```cmd
   deactivate
   ```

---

## 4. Git管理と .venv の「非常に都合が良い」関係

Gitでソースコードを管理する際、**.venv環境を使うことはほぼ必須のルール**となっています。その理由は以下の通りです。

### 🚫 ① 何百メガバイトもの不要データをGitに上げずに済む
`.venv` フォルダの中身（インストールされたライブラリの実体）は非常にファイル数が多く、容量も数百MBになります。これをそのままGitに上げると、同期（プッシュやプル）が非常に遅くなり、Gitサーバーの容量を圧迫します。
* そのため、本プロジェクトの [.gitignore](file:///c:/Users/フォーレスト026/MyProject/.gitignore) ファイルの12行目には、すでに以下のように記述され、`.venv` フォルダをGitの追跡対象から自動的に除外する設定になっています。
  ```text
  .venv/
  ```

### 📄 ② 「レシピ」だけをGitに上げて共有できる
ライブラリの実体を送る代わりに、[requirements.txt](file:///c:/Users/フォーレスト026/MyProject/requirements.txt) という「必要なライブラリ名とバージョンのリスト（レシピ）」だけをGitにコミットして全員で共有します。
* **他の人が同じ環境を再現する手順**:
  1. Gitからソースコードをクローンする。
  2. 自分のローカルPCで空の仮想環境を作る（`python -m venv .venv`）。
  3. レシピを元に一括インストールする（`pip install -r requirements.txt`）。
  これだけで、開発者と全く同じ動作環境が手元のPCに再現されます。

### 🔄 ③ 納品や本番サーバーへのデプロイが劇的に楽になる
本番環境や他のユーザーのPCにツールを移行する際も、「レシピ」があるおかげで、動作環境の構築ミス（「あのライブラリが入っていなくて動かない」など）を100%防ぐことができます。
