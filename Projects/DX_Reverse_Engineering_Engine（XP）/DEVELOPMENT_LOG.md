# DX_Reverse_Engineering_Engine (Windows XP版) 開発記録

本ファイルは、Windows XP向け軽量データコレクター（ロガー）の開発履歴や、発生した課題、およびその解決策を時系列で記録するためのファイルです。

---

## [2026/08/18] プロジェクト立ち上げ・アーキテクチャの決定

### 決定事項
*   **XP端末側の役割:** AI解析や重い処理（Excel解析、PPT生成）を一切行わず、ユーザーの操作ログを取得してCSVに書き出す純粋な「テレメトリコレクター」として動作させる。
*   **サーバー（インターネット側）の役割:** XP端末から出力されたCSVを取り込み、既存の `DX_Reverse_Engineering_Engine` に食わせてAIで解析・可視化を行う。
*   **開発の方向性:** セキュリティを考慮し、生のキーロガー（全キー入力記録）は原則避け、「打鍵数」と「クリップボード」から作業の重さを量るアプローチを基本とする（要件定義参照）。

### 本日の作業内容
1.  既存の `DX_Reverse_Engineering_Engine` の `activity_logger.py` および `TaskMiningClient` の仕様を確認。
2.  XP向けの最適解として、データ収集とデータ解析の分離アーキテクチャを立案。
3.  本開発用ディレクトリ `DX_Reverse_Engineering_Engine（XP）` の作成。
4.  要件定義書 (`REQUIREMENTS.md`) と本開発記録 (`DEVELOPMENT_LOG.md`) の初期化。

### 次のアクション
*   ユーザーと開発言語（C# .NET 2.0/4.0 または C++）を最終決定する。
*   XP端末のベースとなるウィンドウ監視＆キー/マウスフックのプロトタイプ実装を開始する。
